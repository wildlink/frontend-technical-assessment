import React from 'react';

interface CashbackBadgeProps {
  rate: number | null;
}

// TODO: Implement this component.
export function CashbackBadge({ rate }: CashbackBadgeProps): React.ReactElement | null {
  return null;
}
